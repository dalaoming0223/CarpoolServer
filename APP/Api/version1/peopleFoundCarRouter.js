const Router = require('koa')

const { handleResult } = require('../../../Core/callSuccessed')
const router = new Router({
    prefix: '/v1/peoplefindcar'
})


//上传订单信息
// router.post('/upload', async(ctx) => {
  
// })

module.exports = router

